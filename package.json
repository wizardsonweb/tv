{
  "name": "nodestudy",
  "version": "1.0.0",
  "author": "Egon Adelsberger",
  "description": "just a study",
  "keywords": [
    "node, terminal, jsx, mysql, three"
  ],
  "main": "/dev/init.js",
  "scripts": {
    "install": "cd dev && npm install && cd db && node db.js && cd ../client && npm install && cd .. && echo '\n Install finished. \n Run: npm start \n'",
    "start": "cd dev && nodemon init.js && start http://localhost:4307/"
  },
  "license": "ISC"
}
